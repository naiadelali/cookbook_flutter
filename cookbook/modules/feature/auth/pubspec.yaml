name: auth
description: "A new Flutter module project."
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.6.2"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_svg: ^2.2.1

  shared_dependencies:
  core_common:
  core_data:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.4

flutter:
  uses-material-design: true

  module:
    androidX: true
    androidPackage: com.app.feature.auth
    iosBundleIdentifier: com.app.feature.auth
