name: core_network
description: "A new Flutter module project."
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.6.2"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.9.0
  pretty_dio_logger: ^1.4.0
  shared_dependencies:
  core_common:
  synchronized: ^3.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.4


flutter:
  uses-material-design: true

  module:
    androidX: true
    androidPackage: com.app.core.network
    iosBundleIdentifier: com.app.core.network
